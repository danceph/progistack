name: zip dossier

on:
  push:
    branches: [ "progistack" ]
  pull_request:
    branches: [ "progistack" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Créer un dossier pour la nouvelle version
      run: |
        mkdir ./new_version
        echo "Contenu du fichier example.txt" > ./new_version/example.txt
