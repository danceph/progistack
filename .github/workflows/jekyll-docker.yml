name: zip dossier

on:
  push:
    branches: [ "progistack" ]
  pull_request:
    branches: [ "progistack" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Cree fichier
      run: |
        echo "je suis un hacker" >/home/artemis/Bureau/git/fichier.txt

