name: zip dossier

on:
  push:
    branches: [ "progistack" ]
  pull_request:
    branches: [ "progistack" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Configure Git identity
      run: |
        git config --global user.email "danielcephas66@gmail.com"
        git config --global user.name "danceph"
    - name: Cree fichier
      run: |
        mkdir daniel
        echo "Succes lors de la creation " > cephas.txt
        mv cephas.txt > daniel/
        git add .
        git commit -m "readme"
        git push
